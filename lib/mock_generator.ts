import { db, Staff, Order } from '@/lib/db';
import { addDays, subDays, setHours, setMinutes, getDay } from 'date-fns';

/**
 * MOCK DATA GENERATOR 🧪
 * DISABLED: This generator has been neutralized to prevent "Ghost Data" injection.
 */

export async function generateMockData(baseDate: Date = new Date()) {
    console.log("🌱 MOCK DATA GENERATION IS DISABLED. No data will be added.");
}
